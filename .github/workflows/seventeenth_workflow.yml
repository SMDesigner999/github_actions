name: seventeenth workflow
on:
  workflow_dispatch:
    inputs:
      title:
        required: false
        type: string
        default: "hello world"
        description: " custom Title"
jobs:
  run-first-command:
    uses: ./.github/workflows/reusable_workflow.yml
    with:
      title: ${{inputs.title}}
  run-first-commands:
    uses: SMDesigner999/workflows/.github/workflows/blank.yml@main
    with:
      title: ${{inputs.title}}
