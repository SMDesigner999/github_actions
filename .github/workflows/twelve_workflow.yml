name: twelve workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    environment: "PROD"
    steps:
      - name: event vars
        run: |
          echo '${{vars.STATUS}}'  
          echo '${{secrets.PASS}}'
