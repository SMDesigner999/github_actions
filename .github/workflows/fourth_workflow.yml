name: Fourth workflow
on:
  workflow_run:
    workflows: ["third workflow"]
    types:
      - completed

jobs:
  hello-command:
    runs-on: ubuntu-latest
    steps:
      - name: first_step
        run: echo "hello"
