name: fifth_workflow
on:
  issues:
    types: [edited, opened, deleted]
  pull_request:
    types: [assigned, closed]

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: event vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
